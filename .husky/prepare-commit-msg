# Validate commit message format FIRST (before any other checks)
echo "💬 Validating commit message format..."

# Check if this is an interactive commit (has a message file)
if [ -f "$1" ]; then
  npx --no-install commitlint --edit "$1"
else
  echo "⚠️  No commit message file found, validation will happen later"
fi